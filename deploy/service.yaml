apiVersion: v1
kind: Service
metadata:
  name: tsapi-service
  namespace: tsnext
spec:
  selector:
    app: tsapi
  type: NodePort
  ports:
  - name: tsapi-service-port
    protocol: TCP
    port: 1729
    targetPort: 1729
